# Codex Implementation Prompt

Work in the existing `uopcoffee/pokemon-scavenger-hunt` repository.

Before changing code:

1. Read root `AGENTS.md`.
2. Read every file in `/docs` in numeric order.
3. Inspect `README.md`, `data.js`, `screens.jsx`, `components.jsx`, `minigames.jsx`, `styles.css`, and `index.html`.
4. Confirm the current Git branch is `feature/creekside-v2`.
5. Confirm that `archive/v1-original` and tag `v1.0-original` exist. If not, stop and provide the exact commands to create them.

Then produce an implementation plan mapped to the existing files. Do not write code in the first response.

The plan must cover:

- chapter/scene state model
- localStorage persistence
- physical challenge adult press-and-hold completion
- hidden Parent Mode
- four physical code fragments without storing the actual code
- Professor Oak pool chapter and return checkpoint
- Team Rocket basketball chapter
- Secret Ranger Vault
- Victory Road and Champion
- fake credits and Mew epilogue
- reward registry and gift allocation
- offline/no-build preservation
- test and rehearsal strategy

Call out any design requirement that cannot be implemented cleanly in the current static architecture. Do not propose a framework migration unless absolutely necessary.
